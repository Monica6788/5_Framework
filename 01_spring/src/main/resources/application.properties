spring.application.name=01_spring

# server port (#으로 주석)
server.port=8888

# ViewResolver: 템플릿 엔진을 통해서 뷰를 만들어 응답해주는 객체
# 공통되는 접두사와 접미사를 지정해주는 것! (경로와 파일형식을 제외하고 파일 이름만 적기 위함)
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp