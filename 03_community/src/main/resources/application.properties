spring.application.name=03_community

server.port=8989

# jsp view 경로 설정 (ViewResolver)
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# DB setting
spring.datasource.driver-class-name=oracle.jdbc.driver.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=C##SPRING
spring.datasource.password=SPRING

# mybaits setting
mybatis.mapper-locations=classpath:mappers/**/*.xml
# classpath: src/main/resources까지 가리킴

# Type Alias 패키지 경로 지정
mybatis.type-aliases-package=com.kh.community.member.model.dto

# file upload setting
# Max size per uploading file 업로드 파일 하나당 최대 크기
spring.servlet.multipart.max-file-size=10MB

# 업로드 요청의 최대 크기
spring.servlet.multipart.max-request-size=50MB

# 사용자 정의 설정 - 파일 경로
file.upload-dir=uploads
file.upload-dir.profile=uploads/profile
