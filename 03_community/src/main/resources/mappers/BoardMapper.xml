<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.community.board.model.mapper.BoardMapper">
	<!--
		sql 태그: 여러 select 태그에서 공통으로 쓰이는 sql 조각을 정의
				 해당 조각을 사용하고자 하는 경우 include 태그를 사용
	-->
	
	<sql id="searchWhere">
		<where>
			<if test="category != null and category != '' and category != '전체'">
			<!-- null 체크, 빈 string 체크, '전체'일 때는 검색 안 할 거니까 체크 -->
				AND B.CATEGORY = #{category}
			</if>
			<if test="keyword != null and keyword != ''">
			<!-- 검색 키워드가 있는지 검증 -->
				<choose>
					<when test="searchType == 'title'">
						AND B.TITLE LIKE '%' || #{keyword} || '%'
						<!-- 키워드가 포함될 경우 검색 결과에 걸리도록 LIKE 사용 -->
					</when>
					<when test="searchType == 'content'">
						AND B.CONTENT LIKE '%' || #{keyword} || '%'
					</when>
					<when test="searchType == 'writer'">
						AND M.NICKNAME LIKE '%' || #{keyword} || '%'
					</when>
					<otherwise>
						AND (B.TITLE LIKE '%' || #{keyword} || '%'
							OR B.CONTENT LIKE '%' || #{keyword} || '%')
					</otherwise>
				</choose>
			</if>
		</where>		
	</sql>


	<select id="selectBoardListCount" parameterType="BoardSearchCondition" resultType="int">
		select count(*)
		from TB_BOARD B
			left join TB_MEMBER M on B.MEMBER_ID = M.MEMBER_ID
		<include refid="searchWhere" />
		<!-- 지금은 페이징이랑 검색결과 조회에 사용되는 파라미터가 같기 때문에 똑같이 썼는데,
			다른 파라미터를 받아야 하는 경우 sql 태그를 여러 개 작성하고 각각의 DML 태그 안에서
			필요한 sql 조각의 아이디로 include하면 됨 -->
	</select>
	
	<select id="selectBoardList" parameterType="BoardSearchCondition" resultType="BoardDTO">
		SELECT BOARD_ID, B.MEMBER_ID, CATEGORY, TITLE, COUNT, B.CREATE_AT, UPDATE_AT,
			TO_CHAR(B.CREATE_AT, 'YYYY-MM-DD HH24:MI') AS "CREATE_AT_STR",
			nvl(NICKNAME, '탈퇴한 회원') AS "WRITER_NICKNAME"
		FROM TB_BOARD B
			LEFT JOIN TB_MEMBER M ON B.MEMBER_ID = M.MEMBER_ID
		<!-- 마이바티스에서는 조건절도 태그로 작성 가능 -->
		<!-- sql 태그 안에 넣었음! refid: referenceId -->
		<include refid="searchWhere" />
		ORDER BY BOARD_ID DESC
		<!--
			페이징 처리를 위해 OFFSET / FETCH NEXT 구문 추가 (oracle 12c 이상에서 사용 가능)
			이전 버전일 경우에는 rownum이나 rank 함수를 사용해야 함
			: ORDER BY로 정렬된 결과셋에서 특정 행 수를 건너뛰고(OFFSET),
										지정한 행 수만큼 조회(FETCH)
			OFFSET 건너뛸행수 ROWS
			FETCH NEXT 조회할행수 ROWS ONLY	(조회할 행 수는 10개로 고정했)
		-->
		offset #{offset} rows
		fetch next #{size} rows only
	</select>
	
	
	<insert id="insertBoard" parameterType="BoardDTO">
	<!--
		오라클에서 데이터 추가 시 사용된 시퀀스값 추출(조회) 방법
		: selectKey 태그로 삽입 전 시퀀스를 읽어 DTO에 저장하는 방법
	-->
		<selectKey keyProperty="boardId" resultType="long" order="BEFORE">
			select SEQ_BOARD.NEXTVAL from DUAL			
		</selectKey>
		insert into TB_BOARD (BOARD_ID, MEMBER_ID, CATEGORY, TITLE, CONTENT)
		values (#{boardId}, #{memberId}, #{category}, #{title}, #{content})
	</insert>
	
	<insert id="insertBoardImage" parameterType="BoardImageDTO">
		insert into TB_BOARD_IMAGE (IMAGE_ID, BOARD_ID, ORIGINAL_NAME, SAVE_NAME, IMAGE_PATH, IMAGE_ORDER)
		values (SEQ_BOARD_IMAGE.NEXTVAL, #{boardId}, #{originalName}, #{saveName}, #{imagePath}, #{imageOrder})
	</insert>
	
	<update id="increaseViewCount" parameterType="long">
		update TB_BOARD set COUNT = COUNT + 1 where BOARD_ID = #{boardId}
	</update>
	
	<select id="selectBoardDetail" parameterType="long" resultType="BoardDTO">
		select BOARD_ID, B.MEMBER_ID, CATEGORY, TITLE, COUNT, B.CREATE_AT,
				UPDATE_AT, CONTENT, nvl(NICKNAME, '탈퇴한 회원') AS "WRITER_NICKNAME",
				TO_CHAR(B.CREATE_AT, 'YYYY-MM-DD HH24:MI') AS "CREATE_AT_STR",
				TO_CHAR(B.CREATE_AT, 'YYYY-MM-DD HH24:MI') AS "UPDATE_AT_STR"
		from TB_BOARD B
			left join TB_MEMBER M on B.MEMBER_ID = M.MEMBER_ID
		where BOARD_ID = #{boardId}
	</select>
	
	<select id="selectImagesByBoardId" parameterType="long" resultType="BoardImageDTO">
		select IMAGE_ID, BOARD_ID, ORIGINAL_NAME, SAVE_NAME, IMAGE_PATH,
				IMAGE_ORDER, CREATE_AT
		from TB_BOARD_IMAGE
		where BOARD_ID = #{boardId}
		order by IMAGE_ORDER
	</select>
	
	<delete id="deleteBoard" parameterType="long">
		delete from TB_BOARD where BOARD_ID = #{boardId}
	</delete>

</mapper>